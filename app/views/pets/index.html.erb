<h2>Our pets</h2>

<div class="d-flex flex-wrap gap-4" id="pets">
  <% @pets.each do |pet| %>
    <%= link_to render(
      partial: "shared/pet", locals: { pet: pet }
    ), pet %>
  <% end %>
</div>
