<h2>Our pets</h2>


  <div class="d-flex flex-wrap gap-2" id="pets">
    <% @pets.each do |pet| %>
      <%= link_to render(
        partial: "pet", locals: {pet: pet}
      ), pet %>

      <% end %>
  </div>
